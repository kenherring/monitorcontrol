#!/bin/bash
set -eou pipefail

PYTHON=$(command -v python3 || command -v python || echo "py")
if ! command -v "$PYTHON" &> /dev/null; then
    echo "Python is not installed. Please install Python to run this script."
    exit 1
fi

cd "${0%/*}"
"$PYTHON" ../change_mode.py work
